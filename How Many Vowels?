string = input("Enter a word to see how many vowels there are in the word. Word: ")
numvow = 0
for x in string:
    if x=="a" or x=="e" or x=="i" or x=="o" or x=="u":
        numvow=numvow+1
print(numvow)
