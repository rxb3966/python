#Factorial of Factorials

# Function
def fact_of_fact(n):
    fact = 1
    count = 1
    for i in range(1, n + 1):
        fact = fact * i
        count = count * fact
    print(count)

#Main
n = int(input("Enter a number. "))
fact_of_fact(n)
