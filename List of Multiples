#List of Multiples

#Function
def list_of_multiples(num, length):
    for i in range(1, length + 1):
        multi_list.append(num * i)
    print(multi_list)

#Main
num = int(input("Enter the number. "))
length = int(input("Enter the length. "))
multi_list = []
list_of_multiples(num, length)
